FROM postgres:17-alpine

RUN apk add --no-cache restic ca-certificates tzdata

COPY scripts/operations /usr/local/lib/scry/operations
RUN chmod 0555 /usr/local/lib/scry/operations/*.sh

ENTRYPOINT ["/usr/local/lib/scry/operations/backup-entrypoint.sh"]
